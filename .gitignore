# build output
/web/css/app.build.css
/build/
/dist/

# NOTE: web/js/scoring-table.js is generated but committed on purpose, so a
# plain checkout runs without a build. CI regenerates it and fails if it drifts.

# local data
*.db
*.db-wal
*.db-shm
/data/

# tooling
/bin/
/tools/tailwindcss
node_modules/
/e2e/test-results/
/e2e/playwright-report/
coverage.out
*.prof

# env
.env
.env.local

# editor / os
*.swp
.DS_Store
